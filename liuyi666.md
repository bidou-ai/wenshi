# Wenshi 暂存疑问与后续事项

这个文件只保留需要现场讨论或后续实现的问题，正式运行代码不读取它。

## 地图与三块区域

- 温室有三块种植区域，机器人围绕中间区域循环；需要现场提供三个区域的边界截图，以及每条路线段对应的区域和左右侧含义。
- 前期目标身份只按路线段、左右侧、沿路线前后顺序管理；后期要在 Wens1 地图显示坐标，目标约 `+-10cm`。

## 标定与定位

- 前期不做手眼标定；正式地图坐标和跨运行同株关联前，必须补相机、机械臂、底盘之间的外参验证。
- 后退对位初期由现场告诉程序多走/少走多少厘米，逐步形成左右侧参数；后期改为几何计算。

## 水稻交叠标注

- 当前建议按同一个栽植点长出的茎蘖丛作为一株；轻微交叠分别画框，严重交叠无法判断归属时标记 `ambiguous`，不合并成一个框。
- 收集现场正例和反例后，补充带图片的标注规范。

## 花与近景

- 类别保留 `rice=0`、`flower=1`，初期只拍摄和训练 rice。
- 后续小花识别可能需要在完整水稻近景之外增加独立的花近景/微距阶段。

## GPU 与 VMware

- 当前 Ubuntu 运行在 Windows 工作站的 VMware 中，只看到 VMware 虚拟显卡。需要现场提供物理显卡型号，并决定 CUDA 直通、Windows GPU 推理或 CPU 推理方案。

## 跨运行目标关联

- 每次新的巡检生成新的 `run_id` 并刷新本次去重；已拍目标在本次运行内保持2小时去重。
- 是否跨多次运行关联同一株，等地图坐标和手眼标定完成后再决定。
