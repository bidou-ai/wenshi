# 当前状态

## 已完成

- 独立项目目录和 Git 边界已建立。
- 正式路线固定为 `LM1 -> LM4 -> LM3 -> LM2`，地图和路线数学已测试。
- AGV、JAKA、D435 桥、协议、运行日志和固定示教回撤已迁移为 `wenshi_patrol` 包。
- 中文控制台、离线预检、硬件链接检查和唯一启动脚本已具备。
- 图像采集、过期帧拒绝、运行目录隔离和视觉无运动权限边界已具备。

## 当前明确未完成

- 没有经过验证的水稻识别模型。
- 没有正式数据集、标注版本和训练报告。
- 没有已验收的相机内参、手眼标定和 RGB-D 现场报告。
- 固定示教抵近、倒车和视觉识别均默认关闭。
- ROS2 真实硬件联调尚未在本次离线整理中执行。

## 下一步顺序

1. 在 Ubuntu/Windows 分别完成依赖和 `/health` 检查。
2. 空载验证 AGV 状态、JAKA 关节读取和 `goto home` 安全拒绝/通过路径。
3. 在底盘停止时低速复核固定左右示教路径，确认后再单独开启开关。
4. 使用 `collect` 采集并登记数据集，完成模型、标定和离线回放后再评审视觉启用。

