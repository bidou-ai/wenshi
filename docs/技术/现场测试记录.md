# 现场测试记录

## 记录来源与边界

本记录仅整理 `runtime/field_tests/field_test_20260828_034122/` 的 `events.jsonl` 与 `field_test.log`。它不补充日志没有记载的观察，也不代表 32 株、16 点表型现场验收。

## 日志事实

- 教学保存 8/8 个点：`home_safe`、`camera`、`camera_left`、`camera_right`、`left_pre`、`left_photo`、`right_pre`、`right_photo`。
- 教学查询出现 6 次 `timed out`，随后相应点均保存成功。
- 机械臂测试完成 8/8 个点；每次记录的 JAKA `joint_move` 控制响应为 `errorCode: 0`。
- 第一次路线测试启动后未执行路线，结束错误为“AGV 当前急停或阻挡状态未解除”。
- 第二次路线测试完成 8 段：`LM1->LM4`、`LM4->LM3`、`LM3->LM2`、`LM2->LM1` 重复两轮；期间记录一次 `route_blocked_pause` 和一次 `route_blocked_resume`。

## 未由本日志证明的事项

本日志没有记录 32 株 Tag 映射、Tag 实际尺寸或朝向、16 个停车组标定、表型三视角姿态标定、相机/深度/手眼标定、水面补偿、逐株 RGB-D 采集、株高或有效穗人工对照。因此不得用本记录放行正式表型任务。
