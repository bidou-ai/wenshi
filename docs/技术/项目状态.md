# 项目状态

更新时间：2026-08-28。

## 已有离线软件基础

- 配置、预检和调度已采用 32 株、16 个观测组的拓扑；每组两株、每株恰好一次覆盖。
- 表型运行目录、三视角媒体、帧元数据、trait JSON 和复核数据可由硬件无关模块处理。
- 株高支持三维路径弧长、补偿优先级和人工复核；有效穗支持候选分组、合并、拆分、状态和证据计数。
- 后台可只读查看表型运行并导出 CSV。

## 当前阻塞项

| 项目 | 当前状态 |
| --- | --- |
| 32 个 Tag ID 与株号 | 未确认，配置为 `null` |
| Tag 实际边长、后端与朝向 | 未确认 |
| 逐株卡槽到水面高度 | 未测量 |
| 16 个停车组路线参数 | 未标定，字段为 `null` |
| 三视角表型机械臂姿态 | 未标定、未验收 |
| 相机、深度与手眼标定 | 未形成现场证据 |
| 正式表型运动适配器 | 尚未现场验收 |
| 株高和有效穗精度阈值 | 未定义 |

因此 `phenotyping.enabled` 保持 `false`。离线测试、教学、机械臂测试或路线测试均不等于 32 株表型验收。
