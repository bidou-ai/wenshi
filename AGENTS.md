# 项目维护约定

## 文档

- 不为同一事实维护多份说明；优先更新对应的现行文档。
- 功能、配置、现场测试状态变化时，在同一次变更中更新 README 和受影响的现行文档。
- `docs/` 下的新 Markdown 文件与目录使用中文名称。历史设计、计划和已废弃原型移入 `docs/归档/`，不作为日常操作入口。
- 删除文档前先确认没有现行链接、测试或追溯需求；优先归档而不是直接删除。
- 根目录 `README.md` 是 GitHub 项目入口，只描述当前正式范围，并明确未完成的现场标定和硬件能力。

## 与用户协作

- 每次开始任务先阅读相关代码、数据和文档，再判断任务复杂度，不凭记忆直接下结论。
- 简单且边界清楚的任务由主代理直接完成；只有跨多个独立模块或需要长时间审查时才使用多智能体。
- 共享文件或相互依赖的任务按顺序执行，避免并发冲突。
- 改代码前说明理解、范围、风险和验证方法；用户已确认的事项不重复询问。
- 用户提供的数据、日志和命令由主代理直接检查；不把本机可以完成的分析推回给用户。
- 严格区分离线测试、现场硬件测试、已完成、未完成和推测内容；无证据不写成通过。
- 只处理当前需求，除非用户主动要求，不主动带回已结束的旧需求。
- 输出应结构清楚、足够详细但不重复，并提供可直接复制的命令和下一步。

每次任务开始前按 `docs/思考与协作规范.md` 检查。
