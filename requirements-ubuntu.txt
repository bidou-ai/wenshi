# ROS2 Humble 的 rclpy、消息包和 RViz 请使用系统 apt 安装。
numpy
opencv-python
PyYAML

# Optional visual training/inference dependency. Install on the machine with
# the available CUDA/CPU build; yubei training prints an actionable error when
# it is not installed.
ultralytics
